---
id: T27.7
title: Remove /status slash command conflict
plan: plan-2025-12-sprint-27-stabilization
type: bugfix
priority: P0
complexity: 15
status: pending
sprint: 27
tags: [stabilization, bugfix, commands]
depends_on: []
---

# Objective

Remove our `/status` command which conflicts with Claude Code's built-in `/status` command.

# Implementation Plan

1. Remove .claude/commands/status.md if it exists
2. Remove status.md from cookiecutter template
3. Update documentation if it references `/status`
4. Users should use:
   - Claude Code's built-in `/status` for session status
   - `bpsai-pair status` CLI for project status

# Acceptance Criteria

- [ ] No `/status` command in `.claude/commands/`
- [ ] No `/status` command in cookiecutter template
- [ ] Documentation updated if it references `/status`

# Verification

```bash
ls .claude/commands/
# Should NOT show status.md

ls tools/cli/bpsai_pair/data/cookiecutter-paircoder/{{cookiecutter.project_slug}}/.claude/commands/
# Should NOT show status.md
```
