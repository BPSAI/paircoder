Read `.claude/skills/paircoder-task-lifecycle/SKILL.md` then run:
```bash
bpsai-pair task update $ARGUMENTS --status in_progress
```

Then read `.paircoder/tasks/$ARGUMENTS.task.md` and begin work following TDD.
